var expect = require('expect');

import FavorPage from 'components/FavorPage.jsx';

describe('FavorPage', () => {
    // Basic Test
    it('component exists', () => {
        expect(FavorPage).toExist();
    });
});

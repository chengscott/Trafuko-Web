var expect = require('expect');

import PostForm from 'components/PostForm.jsx';

describe('PostForm', () => {
    // Basic Test
    it('component exists', () => {
        expect(PostForm).toExist();
    });
});

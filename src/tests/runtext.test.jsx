var expect = require('expect');

import Runtext from 'components/runtext.jsx';

describe('Runtext', () => {
    // Basic Test
    it('component exists', () => {
        expect(Runtext).toExist();
    });
});

var expect = require('expect');

import RankPage from 'components/RankPage.jsx';

describe('RankPage', () => {
    // Basic Test
    it('component exists', () => {
        expect(RankPage).toExist();
    });
});
